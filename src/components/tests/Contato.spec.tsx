import { render } from "@testing-library/react"
import Contato from "../Contato"

describe('Contato',()=>{

    it('Should have a title with name Contato', ()=>{
        render(<Contato/>)


    })

})